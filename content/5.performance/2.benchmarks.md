---
title: Benchmarks
description: Run the DBCube benchmark suite yourself in four commands. Identical schema, identical data, identical operations, same machine and connection budget — against Prisma, Drizzle, TypeORM and Knex on real PostgreSQL.
navigation:
  icon: i-lucide-bar-chart-3
seo:
  title: Benchmarks — Run them yourself
  description: Reproducible, apples-to-apples DBCube benchmarks against Prisma, Drizzle, TypeORM and Knex on real PostgreSQL 16.
---

## Philosophy

No synthetic mocks, no cherry-picking. The suite lives in the
[`benchmarks/`](https://github.com/Dbcube) folder and runs DBCube against
**Prisma, Drizzle, TypeORM and Knex** on a **real PostgreSQL 16** database with
identical schema, identical data, identical operations, the same machine and the
same connection budget. Every contender is ~80 lines you can read in
`src/contenders/*.js`.

## Run it in five commands

```bash
cd benchmarks
npm install
npm run db:up          # self-contained PostgreSQL 16 via docker compose (port 30046)
npm run prepare:db     # create + seed the bench database (1,000 users, 5,000 orders)
npx prisma generate    # Prisma needs its generated client
npm run bench          # full run (~3-4 min)   |   npm run bench:quick for a fast pass
```

`npm run bench` re-seeds the database first, so every run starts from the exact
same state. Results land in two files:

- `results/latest.md` — human-readable table
- `results/latest.json` — raw numbers + environment metadata (Node version, OS,
  library versions, p95, throughput)

::callout{type="info"}
The benchmark ships its own `docker-compose.yml` (PostgreSQL 16 on port
**30046**). Don't want Docker? Point the whole suite at any PostgreSQL with the
`PGHOST` / `PGPORT` / `PGUSER` / `PGPASSWORD` / `PGDATABASE` environment
variables — every contender reads the connection from one file, so they can't
drift apart.
::

## What we measure, and why

| Operation | Why it matters |
|---|---|
| SELECT by primary key | The single most common query in any app |
| Filtered list (WHERE + ORDER + LIMIT) | Every list / table view in a product |
| COUNT with filter | Dashboards, pagination totals |
| Relation load (50 users + their orders) | The N+1 killer — batched eager loading |
| INSERT single row | Form submissions, API writes |
| UPDATE by primary key | The standard mutation |
| Transaction (2 atomic updates) | Money movement, inventory — the correctness path |
| Bulk INSERT 1,000 rows | Imports, ETL, seeding |
| 100 concurrent PK lookups | Real traffic is parallel, not sequential |

**Reported metrics**: median latency, p95 latency, throughput (ops/s). Warmup
iterations are excluded; connection pools are hot before timing starts.

## Fairness rules

1. **Same database** — one PostgreSQL 16 instance, one schema from
   `src/prepare-db.js` (plain SQL). No ORM-specific migrations.
2. **Same data** — 1,000 users + 5,000 orders, seeded deterministically.
3. **Same connection budget** — every contender is capped at a pool of **10**,
   including Prisma (pinned with `connection_limit=10`; its default would be
   `num_cpus * 2 + 1`, an unfair edge).
4. **Same code shape** — each operation is the *idiomatic* form for that ORM
   (`findUnique` for Prisma, `find()` for DBCube, …).
5. **Warm before timing** — a large warmup phase (excluded) fills pools and
   prepared-statement caches for *every* contender before the clock starts.
6. **No accumulation / order bias** — the bulk-insert table is truncated outside
   the timed region, and the single-insert test resets `users` to its seeded
   1,000 rows before each contender.
7. **Latest stable versions** — recorded in the results file.

## Results

Full run · PostgreSQL 16 (Docker) · Node 22 · Windows x64 · pool ≤ 10. Latency in
ms, lower is better, **bold** = fastest of all five.

| Operation | DBCube | Prisma | Drizzle | TypeORM | Knex |
|---|---:|---:|---:|---:|---:|
| SELECT by primary key | **1.15** | 1.85 | 1.27 | 1.24 | 1.25 |
| Filtered list (WHERE+ORDER+LIMIT 20) | **1.10** | 1.53 | 1.17 | 1.16 | 1.31 |
| COUNT with filter | **0.83** | 1.09 | 1.01 | 1.04 | 1.12 |
| Relation load: 50 users + orders | 4.00 | 5.31 | 3.34 | 3.69 | **3.12** |
| INSERT single row | 1.64 | 2.00 | 1.72 | **1.59** | 1.72 |
| UPDATE by primary key | 1.69 | 2.44 | 1.62 | 1.93 | **1.53** |
| Transaction (2 atomic updates) | **2.15** | 4.70 | 4.07 | 5.58 | 4.55 |
| Bulk INSERT 1,000 rows | 16.48 | 23.63 | 29.52 | 13.57 | **7.40** |
| 100 concurrent lookups (per round) | **8.29** | 9.35 | 24.11 | 22.97 | 18.18 |

::callout{type="warning"}
These are **our numbers on our machine**. They move with hardware, OS, Docker
settings and background load — treat them as a starting point, not gospel. The
whole point of publishing the code is so you can reproduce them on yours.
::

### Deltas vs Prisma (same run)

DBCube is faster than Prisma in **all 9 operations**:

| Operation | Δ vs Prisma |
|---|---|
| SELECT by primary key | **−38%** |
| Filtered list | **−28%** |
| COUNT with filter | **−24%** |
| Relation load | **−25%** |
| INSERT single row | **−18%** |
| UPDATE by primary key | **−31%** |
| Transaction | **−54%** |
| Bulk INSERT 1,000 | **−30%** |
| 100 concurrent lookups | **−11%** |

## Reading the numbers honestly

**Where DBCube leads:**

- **Reads** — primary-key lookups, filtered lists and counts are the fastest of
  all five. This is the bulk of what most apps actually do.
- **Transactions** — fastest by a wide margin. `db.batch()` ships begin + writes
  + commit as **one round-trip**; the others pay several.
- **Concurrency** — 100 parallel lookups scale cleanly instead of serializing.
- **Versus Prisma** — faster on every operation in this run.

**Where DBCube is NOT first — no spin:**

- **Bulk INSERT** — **Knex wins** (~7ms). It's a thin layer over the raw driver
  with no plan/validation step, so a tight `INSERT` loop is hard to beat. DBCube
  (~16ms) still beats Prisma (~24ms) and Drizzle (~30ms).
- **Relation load / single update** — Knex/Drizzle/TypeORM edge ahead; DBCube
  still comfortably beats Prisma.
- **Single insert** — a near tie among the leaders (within ~0.1ms). Treat ties
  as ties.

If your run disagrees with ours, that's expected and useful — hardware differs.
This benchmark is part of our engineering loop, not a marketing asset: every
number is produced by the commands above, on code you can read.

::callout{type="warning"}
Run benchmarks on a **quiet machine**. A background compile or Docker build will
inflate p95 across *all* contenders and make the comparison meaningless. Prefer
comparing **deltas within the same run**, not absolute numbers across runs.
::

::callout{type="info"}
Benchmarking against a **remote/cloud** database instead? The network round-trip
will dominate and every ORM will look the same — that's physics, not the engine.
Read [Where speed shows](/performance/where-speed-shows) to measure fairly and
interpret the numbers correctly.
::

## How the runner works

`src/run.js` orchestrates everything:

1. Warms each contender's connection pool (excluded from timing).
2. For each operation, runs N timed iterations per contender, interleaved to
   spread out transient system noise.
3. Computes median / p95 / ops-per-second.
4. Writes `results/latest.md` and `results/latest.json`.

Each contender implements the same nine operations behind a tiny interface, so
adding a new library to the comparison is one file in `src/contenders/`.
